// non static= they are not change
//non static data member or member function of a class are atributes of object
// static keyword has no object is required // it is shared there value // it is not made another copy//it is hold updated value//overide the prvious value
//static data memeber only declare in the class
// and initilaize after the class //};class name::datatype datatypename//like==>cybrom::int a;//by default it takes 0 if you dont give any value;
//static==always take designing time memory but in
//non static==always take space in compile time//
// non staic==wastage of memory;
// static function support static datatype and same as viveversa for non static
//======by sir
/*
 static member declared only in class and memory always initalize outside the class
 
*/
#include<iost